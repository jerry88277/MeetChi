# ============================================
# Whisper-MLA — Dependencies
# For SVD conversion, fine-tuning, and inference
# ============================================

# Core (RTX 5090 Blackwell needs >=2.5; install via cu126 wheel separately)
torch>=2.5.0
torchaudio>=2.5.0
transformers>=4.40.0
safetensors>=0.4.0

# SVD & Numerics
scipy>=1.12.0
numpy>=1.24.0

# Training
datasets>=2.18.0
accelerate>=0.27.0
tensorboard>=2.16.0
evaluate>=0.4.0
jiwer>=3.0.0

# Audio
soundfile>=0.12.0
librosa>=0.10.0

# Utils
tqdm>=4.66.0
