# MeetChi 統一任務追蹤文件

**最後更新**: 2026-04-13  
**文件版本**: 7.2（Phase 10 & 資源管控、錄音前處理、全域佇列指示完成）

---

## 📊 專案整體狀態俯瞰 (Dashboard)
目前 MeetChi 已經成功完成從 MVP (SaaS 替代品) 走向企業級應用架構的轉型。基礎雲端設施 (Cloud Run + PostgreSQL + Cloud Tasks) 以及前端模組化架構均已就位。
- **當下重大瓶頸**：L4 GPU 上的並行性限制（KV Cache 容量耗盡）。
- **當前突破口**：進行 Phase 10 的 Whisper-MLA 架構升級，解決 GPU OOM 問題以提升 10 倍併發處理力。

---

## 🚀 一、當下焦點任務 (In Progress WIP)

### Phase 10: Whisper-MLA ASR 引擎升級
> **目的**：將 MHA 改為 MLA 壓縮 KV Cache (5.1KB → 0.48KB/token)，從單路並發提升到 5-10 路並發。
- **10.0~10.1 環境準備與 SVD 轉換**：✅ 已完成（重建誤差 < 5%）
- **10.2 MLA Attention 模組測試**
  - [x] 實作 `fused_mla.py` (整合至 Transformers DynamicCache)
  - [x] 撰寫 `test_fused_inference.py` 並運行對齊驗證
  - [ ] 驗證：forward pass cosine similarity > 0.95
- **10.3 Fine-tune 階段 (目前主要卡關點/解決中)**
  - [x] 修復 Windows 多進程內存溢出 (WinError 1455)
  - [x] 開發離線訓練啟動腳本
  - [ ] 執行 Fine-tune（~48-72hr on RTX 5090）
  - [ ] 驗證中文 CER ≤ Breeze + 1%、英文 WER ≤ Breeze + 1%、CS MER ≤ Breeze + 2%
- **10.4 MeetChi GPU Service 整合**
  - [ ] 修改 `offline_asr.py` 新增 WhisperMLAProvider
  - [ ] 修改 `Dockerfile.gpu-service` 與 `requirements-gpu.txt`
  - [ ] 本機整合測試 + E2E 驗證
- **10.5 GCP 部署**
  - [ ] 上傳 Fine-tuned MLA 模型至 GCS
  - [ ] 修改 `cloudrun-gpu-service.yaml`（`containerConcurrency: 1 → 10`）
  - [ ] Build & Push Docker Image → 佈署與併發壓力測試

### Phase 11: Pure Web 邊緣推論與 VDI 資源防護架構
> **目的**：拋棄桌面框架，全面採納 Edge-Cloud Hybrid 端雲混合架構。實現零伺服器成本推論、硬體智能降級、與全網頁側錄。
- **11.1 前端核心整合與邊緣推論**
  - [ ] 導入 `@huggingface/transformers.js` 或 `onnxruntime-web`。
  - [ ] 實作專屬 Web Worker 初始化本地量化模型 (Whisper-tiny/base)，隔離 React UI 執行緒以防畫面卡頓。
  - [ ] (移植自遠期規劃) AudioWorklet 內核遷移 + 本地 RNNoise 降噪。
- **11.2 環境嗅探與全端動態降級 (Fallback Routing)**
  - [ ] **極速層**：試探 `navigator.gpu` 啟動 WebGPU 極速推論。
  - [ ] **護城河層 (針對 VDI 等受限環境)**：若降級至 Wasm 模式，偵測 `hardwareConcurrency`。若 CPU 核心數 ≤ 4 核，精準強制降頻 (設定 `wasm.numThreads = 3`)，死守 1 顆核心給作業系統與 UI Event Loop，防堵 VDI 滑鼠失幀與崩潰。
  - [ ] **雲端備援層**：若硬體完全無力負荷，觸發防禦性降級，將音訊改拋至 WebSocket 走 GCP 雲端推論。
- **11.3 系統側錄功能開通 (Web-based Capture)**
  - [ ] (移植自遠期規劃) 新增 `getDisplayMedia` API 呼叫，允許捕獲 YouTube 或第三方線上會議之「系統音效 (System Audio)」直接轉錄。

---

## 📝 二、待辦清單 (Backlog / To-Do)

### 🔴 P0: 核心阻礙與韌性防禦 (Critical Blockers)
- [x] **Web 端 WebSocket 錄音整合**
  - `MediaRecorder` 捕捉麥克風 → WebSocket (`/ws/transcribe`) 傳送音訊串流。
  - 停止錄音時正確儲存 meeting + segments + audio_url。
- [x] **[UX 本質] 斷線容錯防護 (Resilience UX)** ✨
  - 在 `RecordingView` 加入 IndexDB 暫存音訊 Blob chunks 架構。
  - 確保網路瞬斷或休眠時，會議錄音不遺失，連線恢復時接續斷點上傳。
- [x] **音訊播放功能基礎**
  - 後端 audio streaming/download API。
  - 前端 `<audio>` player 元件。

### 🟡 P1: 使用者體驗與高階功能 (UX & Core Features)
- [x] **[UX 本質] 非同步排隊狀態列 (Queue Status & Skeleton)** ✨
  - 於全域 Dashboard 導入處理中佇列指示器，透過狀態共用管理，消弭處理期間切換畫面的焦慮。
- [x] **[UX 本質] Audio-Text 雙向同步連動 (Playback Sync)** ✨
  - RAG 原文對照最後一哩路：點擊 `DetailView` 逐字稿或右側 Citation 面板中特定段落時，播放器自動跳轉對應秒數。
- [x] **進階錄音前處理**
  - 使用者自訂專有名詞/關鍵字輸入 (作為 LLM Prompt Context)。
  - 上傳前由前端 `Web Audio API` 解析音檔長度，避免損耗後端 I/O。
- [ ] **Phase 8.3 跨會議記憶宮殿 (部分優先)**
  - ✨ Meta-Prompting 優化按鈕（AI 協助改寫指令）。
  - AI 模板推薦（逐字稿掃描後推薦模板）。
  - 角色導向多視角摘要 (Role-Aware Summarization)。
- [ ] **企業資安：防護與權限控制 UI**
  - 簽核工作流 UI (Request/Approve)。
  - 前端基礎防護（`user-select: none`, 攔截 Ctrl+P/Ctrl+S/ContextMenu）。
  - DOM 動態浮水印實作 (隨登入者工號變動)。
  - 防篡改監聽 (MutationObserver)，遇竄改立刻強制登出。

### 🔵 P2: 遠期規劃與整合 (Nice to Have / Future)
- [ ] **跨會議長時記憶整合**
  - 模板 Marketplace (組織內分享)。
  - 跨會議講者記憶 (Speaker Profile DB) 與空間標籤連動。
- [ ] **後端企業資安落地**
  - 資料庫 `pgvector` 上的 Row-Level Security 存取管制。
  - 極短效 Signed URL 音檔發放。
- [ ] **平台整合與拓源**
  - Phase 9.3: TeamsChannel webhook 實作與 Teams Bot 註冊。
  - Sprint 6.5: NextAuth Azure AD (Entra ID) 整合與 RBAC 權限管理。
- [x] **[UX 本質] 機密視覺印記 (Trust Badge)** ✨
  - 在前端清楚呈現「本場會議為地端/離線模型處理，無外部連線」的高信任盾牌圖示。

---

## ✅ 三、已完成任務歸檔 (Archive / Done)

> 為了保持版面整潔，已合併並收攏過去 Sprint 的完成軌跡。

### 🏗️ 基礎設施與後端平台 (Infrastructure & Data Layer)
- **Phase 10 (2026-04)**：實作成本守門限流機制 (Anti-overlap + Cloud Run max-instances=1)，保護基礎設施避免無限制觸發，並降低單一服務異常引發之爆炸成本。
- **Phase 6.1~6.4 (2026-03)**：DB 遷移到 Cloud SQL PostgreSQL，啟用 `pgvector` 擴充。修復 PyTorch 與 Webhook 衝突，並以 Cloud Tasks 完全解耦摘要與轉錄 API，徹底解決 503 Gateway Timeout 事故。
- **GCP 基礎佈署**：Terraform 自動化佈建 (Cloud Run CPU/GPU、Secret Manager)、Docker 映像管理，以及身份驗證 (Service Account)。

### 🧠 模型推理與 ASR 引擎 (GPU & AI Layer)
- **Sprint 5.2 & 5.3 (2026-02)**：完成離線 Map-Reduce 長文本摘要。導入 Hugging Face Token，使用 pyannote 結合 WhisperX 進行講者 Diarization。
- **即時串流**：Silero VAD 端點偵測與 WebSocket 字元級段落映射 (Smith-Waterman)。

### 🌐 前端架構與介面框架 (Frontend & UI Framework)
- **Phase 9 (2026-03)**：完備 Event-Triggered Polling，實作 Exponential Backoff 更新 UI 狀態，並與 Discord Webhook 結合。
- **Phase 8.1~8.3 (2026-03~04)**：Schema-Driven 模板引擎與 Template Gallery；實作講者編輯列 (Speaker Mapping Bar) 結合 OWASP LLM 01 防護；完成跨會議即時問答檢索 (RAG) 的端到端建構（pgvector 整合、全螢幕 Workspace、與返回控制台的 UX 修補）。
- **Phase 7 (2026-03)**：將龐大的單檔 `page.tsx` 拆分為 hook 與獨立組件，並導入奇美色彩指南的 CSS Variables 系統與 Dark Mode 主題設計。
- **原生客戶端與基礎 Dashboard**：已完成 Tauri 桌面版開發、NextAuth.js 整合與各類靜態管理頁面。

---

## 📚 四、決策基準與顧問報告 (References)
> 重大開發任務背後的策略考量可見此索引。

| 領域 | 報告名稱 | 重點內容 |
| --- | --- | --- |
| **架構決策** | `llm_wiki_vs_mempalace.md` | 捨棄 LLM Wiki 改採 pgvector 記憶宮殿。 |
| | `consult_region_strategy_20260308.md` | Cloud Run GPU 區域部署決策。 |
| | `asr_mece_evaluation.md` | Whisper-MLA vs 其他語音辨識方案的抉擇點。 |
| | `whisper_mla_deep_dive.md` | KV cache 壓縮與 SVD 轉換原理。 |
| | `implementation_plan.md` (03-18)| Whisper-MLA 轉換的原始詳細版實作計畫。 |
| **前端體驗** | `frontend_analysis.md` | 本質分析：Audio Sync、斷線防護與排隊焦慮 (Phase 10 UX 核心)。 |
| | `consult_frontend_reactivity_20260312.md` | Event-Triggered Polling 狀態機設計。 |
| | `consult_summary_templates_20260312.md` | 模板引擎系統 (Schema-Driven) 與市場比較。 |
| **後端與資安**| `consult_envvar_incident_20260312.md` | 環境變數覆蓋事故的根因修復分析。 |
| | `consult_template_optimization_security_20260312.md` | Prompt Injection 阻絕與 OWASP LLM01 攻擊防禦策略。 |
| | `consult_discord_webhook_20260312.md` | Discord Webhook MVP 通知串接指南。 |