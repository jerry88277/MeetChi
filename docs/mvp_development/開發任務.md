# MeetChi 統一任務追蹤文件

**最後更新**: 2026-03-12  
**文件版本**: 4.4（Phase 8 智能摘要引擎統合規劃）

---

## 一、已完成項目

### Phase 1: 桌面客戶端 (Tauri Client) — ✅ 已完成，進入維護
- [x] 即時轉錄模式（WebSocket + Silero VAD + 串流字幕）
- [x] 對齊模式（Smith-Waterman + 字元級段落映射 + Global Resync）
- [x] 雙視窗架構（Overlay + Settings 獨立視窗）
- [x] 設定功能（音訊來源/語言/翻譯/字體/Initial Prompt）
- [x] 講稿編輯器（雙窗格 + 自動行號）
- [x] 關鍵字校正系統（corrections.json）

### Sprint 5.1: 資料持久化與歷史列表 — ✅ Completed
- [x] DB Schema（Meeting, TranscriptSegment, Summary — SQLAlchemy）
- [x] Backend CRUD API（Create/List/Detail/AddSegments）
- [x] Tauri 歷史頁面 + API Client + 儲存流程整合

### Sprint 5.2: 離線摘要與模板 — ✅ Completed
- [x] LLM Service `/summarize`（Map-Reduce 長文本）
- [x] PromptTemplate 管理（General/Sales BANT/HR STAR/RD）
- [x] BackgroundTasks 非同步摘要（Celery Fallback）
- [x] 前端摘要面板 + 模板選擇 + Polling 狀態更新

### Sprint 5.3: 講者分離 — ✅ Completed
- [x] pyannote.audio 整合（Hugging Face Token）
- [x] 音訊預處理（16kHz Mono WAV）
- [x] WhisperX 整合 Speaker Diarization Pipeline
- [x] 前端逐字稿講者標籤 UI

### GCP 部署基礎設施 — ✅ Completed
- [x] Docker 映像建置推送（backend + llm）
- [x] Cloud Run 部署（backend + llm + frontend）
- [x] ADC 遷移（API Key → Service Account）
- [x] Terraform 基礎設施（Cloud Run + Cloud SQL + Secret Manager）
- [x] 端到端 API 驗證（health / summarize / polish / corrections / meeting CRUD）

### Web Dashboard UI 框架 — ✅ Completed
- [x] OAuth Google 登入（NextAuth.js）
- [x] 會議列表頁（搜尋、狀態顯示、卡片佈局）
- [x] 會議詳情頁（逐字稿 + 摘要分欄佈局）
- [x] 模板展示頁面（唯讀展示後端支援的 4 種模板 + Coming Soon 提示）
- [x] 管理頁面（真實數據：meetings.length / completed count / 後端連線狀態）
- [x] 系統設定頁面（唯讀 disabled toggle + 說明文字「使用後端預設值」）
- [x] Mock 移除：錄音 UI → Coming Soon、Play/MoreVertical 移除、假波形移除
- [x] 刪除會議功能（前端）：handleDeleteMeeting → DELETE API + 成功 banner + 自動導回
- [x] 匯出功能（前端 client-side）：TXT / SRT / JSON（2026-02-10）

---

## 二、進行中 / 待修復

### 🔧 Frontend v10 重建部署
- [x] GCP Frontend 與本地程式碼不一致
  - [x] Cloud Build 重建 `meetchi-frontend:v8`（2026-02-09 mock 移除 + 刪除按鈕）
  - [x] Cloud Build 重建 `meetchi-frontend:v9`（2026-02-10 API 重構 + 排序 + Bug Fix）
  - [x] Cloud Build 重建 `meetchi-frontend:v10`（2026-02-10 刪除 UX 修復 + 匯出功能）
  - [x] Cloud Build IAM 修復（新增 `roles/run.admin` + `roles/iam.serviceAccountUser`）

---

## 三、待辦 — 按優先順序

### 🔴 P0: 錄音 → 轉錄核心流程（Critical Blocker）

> **根本問題**：Web 端錄音頁面純 UI Mock，沒有實際 WebSocket/音訊整合。  
> 這導致：所有會議無音檔、無逐字稿、摘要永遠生不出來。

- [ ] **Web 端 WebSocket 錄音整合**
  - [ ] `MediaRecorder` 或 `AudioWorklet` 捕捉麥克風音訊
  - [ ] WebSocket 連線至 `/ws/transcribe` 傳送音訊串流
  - [ ] 即時接收 ASR 轉錄結果，更新 UI 逐字稿
  - [ ] 停止錄音時正確儲存 meeting + segments + audio_url
- [ ] **音訊播放功能**
  - [ ] 後端 audio streaming/download API
  - [ ] 前端 `<audio>` player 元件

### 🟡 P1: 使用者體驗優化（Sprint 5.4）

- [x] 歷史記錄排序（按 `created_at` 降序）（2026-02-10）
- [x] 刪除會議功能
  - [x] 前端：handleDeleteMeeting → confirm + fetch DELETE（2026-02-09）
  - [x] 後端：`DELETE /api/v1/meetings/{id}`（含音訊+逐字稿一併刪除）
  - [x] 前端：在 DetailView header 新增「刪除」按鈕 UI（Trash2 icon + confirm + loading state）（2026-02-09）
- [x] 匯出功能（TXT, SRT, JSON）（2026-02-10 前端 client-side 完成）
  - [x] 前端：匯出按鈕 + 格式選單（Download icon + hover dropdown）
  - [ ] 後端：PDF / DOCX 轉換 API（待開發）
- [ ] 摘要與逐字稿進階設定
  - [ ] 專有名詞/關鍵字輸入（LLM Prompt Context）
  - [ ] 上傳補充圖片 OCR
  - [ ] **講者標籤修改與 AD 帳號串接**：→ 已納入 Phase 8.1.3 Speaker Mapping Bar
  - [ ] **上傳前前端解析音檔長度**：使用 Web Audio API 解析長度並寫入 DB，避免後端 I/O 損耗。
  - [x] **修復自動摘要未觸發問題 (Webhook 解耦)**：實作 `POST /api/v1/callbacks/asr-done`，讓 GPU ASR 算完後主動打回 CPU Backend，零閒置成本徹底解決 Timeout。（Phase 6.3 完成）
  - [x] **修復摘要完成後 Meeting.status 未更新為 COMPLETED**：`tasks.py:generate_summary_core` 寫入 `summary_json` 後加入 `meeting.status = MeetingStatus.COMPLETED`（成功）/ `FAILED`（失敗）。E2E 驗證通過：PROCESSING → COMPLETED。（Phase 6.4 完成 2026-03-09）
  - [x] **Phase 7.1 前端檔案拆分**：`page.tsx` 1,704 行 → 拆分為 9 個獨立檔案 + 主控制器 ~320 行。Backend v30 + Frontend v5 部署至 Cloud Run 並通過 E2E 驗證。（2026-03-09）
  - [x] **Phase 7.2 State Consolidation**：14 useState → 3 custom hooks（useMeetings/useRecording/useSummary）+ useTheme。page.tsx 463→276 行。（2026-03-12）
  - [x] **Phase 7.3 Brand Colors & Theming**：globals.css 7→25+ CSS 變數（brand-navy/cta/accent/highlight + status + semantic）。6 元件品牌色替換 + Dark Mode toggle + MeetingCard 4px 狀態色條。Build 通過。（2026-03-12）
  - [ ] 摘要長度選項（短/中/長）→ 已納入 Phase 8.2.3 Template Editor
  - [ ] 摘要風格選項（正式/非正式）→ 已納入 Phase 8.2.3 Template Editor
  - [x] 講者/專有名詞自定義對照表（後端已實作硬編碼，待前端 UI）
- [ ] 後端 API 擴展
  - [ ] `PATCH /api/v1/meetings/{id}` (支援更新 duration 等 metadata)
  - [ ] `POST /generate-summary` 擴展動態參數

### 🟢 P2: 功能完善

- [ ] 模板管理 CRUD → 已納入 Phase 8.2（Schema-Driven Template Engine）
- [ ] 系統設定真實化（toggle 開關連接後端）
- [ ] 講者標籤編輯 UI → 已納入 Phase 8.1.3（Speaker Mapping Bar）

### ⚪ P3: 遠期規劃

- [ ] **Sprint 5.5**: Web 端系統音訊採集
  - [ ] `getDisplayMedia` 封裝
  - [ ] Audio Track 提取 + AudioContext 整合
  - [ ] UI「系統音訊（透過螢幕分享）」選項
- [ ] AudioWorklet 遷移 + RNNoise 降噪
- [ ] 帳號管控（Sprint 6.5）
  - [ ] NextAuth.js + Azure AD 整合
  - [ ] RBAC (Role-Based Access Control)
  - [ ] 壓力測試與滲透測試

### 🟣 Sprint 6: 企業資安與基礎設施升級 (架構諮詢實作)

> **架構重構動機 (第一性原理與對話紀錄勾稽)**：
> 1. **503 事故解法**：為解決 GCS FUSE 與 SQLite Byte-range lock 不相容導致的 503 癱瘓，必須捨棄本地無狀態的 DB，轉向完整的 Client-Server DB。
> 2. **未來擴展性**：為接下來的 AI 語意搜尋鋪路 (RAG)，需要原生儲存向量的能耐。
> 3. **不可否認性與防外流**：面對機密會議記錄，Application-level 的權限控管過於脆弱，需下沉至 DB 底層管制，並結合前端嚇阻機制。

#### 1. 基礎設施層 (Infrastructure & Data Layer)
- [x] **重啟 Cloud SQL 部署**：`terraform/database.tf` PostgreSQL 15 + Cloud SQL Auth Proxy（Phase 6.1 完成）
- [x] **啟用 pgvector 擴充**：`main.py` startup 時 `CREATE EXTENSION IF NOT EXISTS vector;`（Phase 6.1 完成）
- [ ] **DB 權限控管 (RLS)**：在 PostgreSQL 啟用 Row-Level Security，建立 `meeting_access_logs` 表
- [ ] **稽核防線 (Trigger)**：建立 `meeting_confidentiality_audits` 表，Outbox Pattern

#### 2. 應用程式層 (Backend Model & API Layer)
- [x] **ORM 驅動與連線池優化**：PostgreSQL + pool_size=5 + pool_pre_ping（Phase 6.1 完成）
- [ ] **Data Model 擴充**：`models.py` 尚未加入 `Vector(768)` 欄位（`CREATE EXTENSION` 已就緒，ORM 欄位待實作）
- [ ] **RLS Session Context 綁定**：待開發
- [ ] **極短效 Signed URL 發放**：待開發
- [ ] **Outbox 通報 Worker**：待開發

#### 3. 前端呈現層 (Frontend UI & Security Layer)
- [ ] **簽核工作流 UI**：於 Next.js 實作機密調閱 Request/Approve 介面。
- [ ] **前端基礎防護**：全域加入 CSS `user-select: none`，並掛載 `contextmenu` 與 `keydown (Ctrl+P, Ctrl+S)` 攔截器。
- [ ] **DOM 動態浮水印實作**：建立高優先層級的 `<svg>` 全畫面 Overlay (帶有調閱者信箱/工號/時間戳)，設定 `pointer-events-none` 且 `z-index: 9999`。
- [ ] **防篡改監聽 (MutationObserver)**：監視浮水印 DOM 節點，一旦遭使用者透過 F12 竄改 (刪除、改透明、隱藏)，立刻攔截觸發黑屏與強制登出。

---

### 🟢 Phase 6.1: DB 遷移到 Cloud SQL PostgreSQL — ✅ 2026-03-05 已完成

- [x] Terraform：建立 `google_sql_database_instance` (`meetchi-db-pg`, PostgreSQL 15, db-f1-micro)
- [x] Secret Manager：`meetchi-db-password`, `meetchi-hf-token`, `meetchi-secret-key`, `meetchi-gemini-api-key`
- [x] Backend `database.py`：PostgreSQL 連線池（pool_size=5, max_overflow=10, pool_pre_ping=True）
- [x] Backend `models.py`：加入 `Vector(768)` 欄位（summary_embedding, content_embedding）
- [x] Backend `main.py`：Startup 時 `CREATE EXTENSION IF NOT EXISTS vector;`
- [x] Terraform `variables.tf`：補充 `gpu_asr_service_url`, `db_password` 宣告
- [x] Cloud Run Backend：更新 DATABASE_URL → Cloud SQL Auth Proxy Unix Socket 連線
- [x] Cloud Run 環境變數修復：補回 GCS_BUCKET, GEMINI_MODEL, GPU_ASR_SERVICE_URL 等

### 🟢 Phase 6.2: GPU ASR Speaker Diarization 修復 — ✅ 2026-03-05 已完成

- [x] **根因 1**：`torchaudio>=2.9.0` 移除 `AudioMetaData` — pin `torchaudio<2.6.0`
- [x] **根因 2**：`huggingface_hub>=1.0` 移除 `use_auth_token` — pin `huggingface_hub<1.0`
- [x] **根因 3**：PyTorch 2.6 改 `torch.load` 預設 `weights_only=True` — pin `torch<2.6.0`
- [x] **網路交叉驗證**：pyannote-audio Issue #1804、HuggingFace Discuss #172402/4、PyTorch Release Notes
- [x] 修改 `requirements-gpu.txt`：`torch>=2.1.0,<2.6.0`、`torchaudio>=2.1.0,<2.6.0`、`huggingface_hub>=0.20.0,<1.0`
- [x] `offline_asr.py`：保留 `use_auth_token=self.config.hf_token`（配合 huggingface_hub<1.0）
- [x] 重建 GPU ASR Docker Image（多次迭代：b6bc4e0e → e8fcfd0b → e595f219 → c376929d）
- [x] 部署 Cloud Run GPU（revision 00023）→ E2E 測試 ✅ `Diarization done: 8 speakers detected`
- [x] HF_AUTH_TOKEN 已確認正確掛載（`os.getenv("HF_AUTH_TOKEN") or os.getenv("HF_TOKEN")`）

| 服務 | Image Tag | 部署時間 | 本地一致性 |
|------|-----------|----------|-----------|
| Frontend | `meetchi-frontend:v7` | 2026-03-10 | ✅ 一致 (pending 狀態顯示) |
| Backend | `meetchi-backend:v36` | 2026-03-11 | ✅ 一致 (error guard + None fallback + max_output_tokens 8192) |
| GPU ASR | `meetchi-gpu-asr:v5` | 2026-03-10 | ✅ 一致 (同步 ASR + inline callback) |
| LLM GPU | `meetchi-llm-gpu:v2` | 2026-02-09 | ✅ 一致 |

---

### 🟢 Phase 6.3: Webhook 非同步回呼 + Cloud Tasks 摘要解耦 — ✅ 2026-03-09 已完成

- [x] GPU ASR 服務新增 `callback_url` 參數與 `httpx` Webhook 回呼機制
- [x] CPU Backend 新增 `POST /api/v1/callbacks/asr-done` Webhook 接收端點
- [x] `callbacks.py` 使用 `google.cloud.tasks_v2` 將摘要排入 Cloud Tasks Queue（取代 BackgroundTasks）
- [x] Terraform 新增 `cloudtasks.enqueuer` IAM 角色給 `meetchi-cloudrun` Service Account
- [x] `tasks.py` 新增 `skip_asr` 參數，防止 `/tasks/summarization` 迴圈觸發 GPU ASR
- [x] `llm_utils.py` 分離 `GEMINI_LOCATION`（`us-central1`）與 `GCP_LOCATION`（`asia-southeast1`）
- [x] `GEMINI_MODEL` 最終確認為 `gemini-2.5-flash-lite`（Vertex AI GA 模型，費用最低）
- [x] E2E 驗證：上傳 → GPU ASR → Webhook 200 OK → Cloud Tasks → Gemini 摘要 200 OK ✅
- [x] 清理 15 個測試 Meeting，保留含摘要的 `e971a65b`

### ✅ Phase 6.4: 摘要狀態 Bug 修復 — 2026-03-09 已完成

- [x] `tasks.py:314` 後加 `meeting.status = MeetingStatus.COMPLETED`
- [x] 失敗路徑 `tasks.py:325` 設定 `meeting.status = MeetingStatus.FAILED`
- [x] 重新部署 + 觸發 summarization 更新既有 Meeting 狀態
- [x] 前端驗證：會議列表顯示「已完成」、詳情頁顯示摘要內容

---

### 🔵 Phase 7: 前端漸進式重構（品牌色彩 + 架構優化）

> **重構動機（第一性原理）**：
> 1. `page.tsx` 1,704 行單檔包含 8 個組件 — 違反單一職責原則
> 2. 30+ 個 `useState` 散落 — 認知負擔高、難以並行開發
> 3. 200+ 處 hardcoded 色彩 — 無法品牌化、不支援 Dark Mode
> 4. 錄音入口 3 處重複 — 違反 DRY、增加使用者認知負荷

#### Phase 7.1: 檔案拆分 — ✅ 2026-03-09 已完成
- [x] 建立 `src/types/meeting.ts`（Types 24 行）
- [x] 抽取 `src/lib/export.ts`（匯出工具 65 行）
- [x] 抽取 `src/lib/transform.ts`（API 轉換 47 行）
- [x] 拆分 `src/components/Sidebar.tsx`（117 行）
- [x] 拆分 `src/components/RecordingView.tsx`（492 行）
- [x] 拆分 `src/components/MeetingCard.tsx`（88 行）
- [x] 拆分 `src/components/DashboardView.tsx`（128 行）
- [x] 拆分 `src/components/DetailView.tsx`（173 行）
- [x] 拆分 `src/components/SettingsView.tsx`（81 行）
- [x] `page.tsx` 縮減至 ~320 行主控制器

#### Phase 7.2: State 整理 + CTA 統一 — ✅ 2026-03-12 已完成
- [x] 抽取 `useMeetings()` hook（meetings, isLoading, error, isConnected）
- [x] 抽取 `useRecording()` hook（recordingMeetingId, isUploading）
- [x] 抽取 `useSummary()` hook（isRegenerating）
- [x] 抽取 `useTheme()` hook（theme/toggleTheme + localStorage + data-theme）
- [ ] 統一「新增會議記錄」CTA：合併「新會議(錄音)」+ 「上傳錄音」為下拉選單
- [ ] Sidebar 「record」tab 整合或移除

#### Phase 7.3: 品牌色彩主題系統 — ✅ 2026-03-12 已完成

> **品牌色來源**：奇美色彩指南（Pantone + RGB + CMYK 完整定義）

- [x] 擴充 `globals.css` CSS Variables：7 → 25+ token（含 Light/Dark Mode）
- [x] 品牌色映射：
  - Sidebar 背景：奇美藍 `#041E42`
  - CTA 按鈕：大海藍 `#2D428B`
  - 成功狀態：森林綠 `#48B070`
  - 處理中：柑橘橙 `#E4831A`
  - 錯誤/刪除：胭脂紅 `#D2343D`
  - Sidebar Active：螢光藍 `#5BFAE6`
- [x] 替換 6 元件 hardcoded Tailwind 色彩 class（Sidebar/MeetingCard/DashboardView/DetailView/SettingsView/page.tsx）
- [x] 實作 `useTheme()` hook + `localStorage` 持久化
- [x] Settings 頁面新增 Dark Mode toggle（取代 disabled 假 toggle）
- [ ] WCAG AA 對比度驗證（所有文字組合 ≥ 4.5:1）
- [x] 會議卡片新增左側 4px 狀態色條（綠=完成、橙=處理中、紅=失敗）
- [x] Frontend v6 部署至 Cloud Run + Smoke Test 通過（2026-03-12）

---

### 🔵 Phase 8: 智能摘要引擎 + 說話者系統

> **統合來源**：4 份架構諮詢報告 + NLM Research（18 學術來源）  
> **詳細計畫**：[consult_phase8_unified_plan_20260312.md](file:///C:/Users/User/.gemini/antigravity/brain/c79abb06-67bb-485f-8cbb-f2a9f2c5af80/consult_phase8_unified_plan_20260312.md)

#### Phase 8.1: 基礎層 — 安全 + 角色推斷 + 講者編輯（Short 1-4h）
- [ ] **8.1.1 Sandwich Defense**：OWASP LLM01 修復 — `llm_utils.py` prompt 組裝加入 `---SYSTEM BOUNDARY---`
- [ ] **8.1.2 CoT 角色推斷**：system_prompt 加入 Chain-of-Thought 指令 + `speaker_roles` Pydantic Schema
- [ ] **8.1.3 Speaker Mapping Bar**：逐字稿面板 inline 編輯 + 8 輔助色自動分配 + `PATCH /speakers` API
- [ ] **8.1.4 Injection Guard**：正則黑名單 + LLM 分類器 + 前端即時警告

#### Phase 8.2: 模板層 — Schema-Driven 引擎 + 前端 Gallery（Medium 1-2d）
- [ ] **8.2.1 Template Engine**：Template JSON Schema + `build_prompt_from_template()` + 10 系統預設模板
- [ ] **8.2.2 Template Gallery**：模板卡片 + 分類 Tab + 範例輸出預覽 Modal
- [ ] **8.2.3 Template Editor**：Fork 自訂模板 + 段落 CRUD + 指令編輯 + 使用者說明

#### Phase 8.3: 增強層 — AI 輔助 + 跨會議記憶（Medium-Large 2-3d）
- [ ] **8.3.1 ✨ Meta-Prompting 優化按鈕**：AI 重寫使用者指令 + diff 預覽 + ✅/❌
- [ ] **8.3.2 AI 模板推薦**：逐字稿 pre-scan → 自動推薦最適模板
- [ ] **8.3.3 跨會議講者記憶**：Speaker Profile DB + 聲紋/名稱比對
- [ ] **8.3.4 AD 帳號自動映射**：Entra ID 整合（依賴 Sprint 6.5）
- [ ] **8.3.5 模板 Marketplace**：組織內模板分享/訂閱
- [ ] **8.3.6 指令優化歷史**：before/after 記錄 → 好指令語料庫
- [ ] **8.3.7 Canary Token**：system_prompt 洩露偵測
- [ ] **8.3.8 角色導向多視角摘要**：不同角色生成不同視角摘要

---

## 五、Dashboard 功能審計摘要（2026-02-09 已清理）

| 分級 | 功能 | 狀態 |
|------|------|------|
| ✅ Real | 會議列表/詳情、OAuth 登入、健康檢查、摘要 API、刪除會議、匯出 TXT/SRT/JSON | 已連接後端 |
| ✅ Honest | 錄音 → Coming Soon 頁面、Settings → disabled toggle、Templates → 唯讀展示 | 誠實標示開發中 |
| ⚠️ 待開發 | WebSocket 錄音串接、音訊播放、匯出、模板 CRUD、Settings 真實連線 | 需後端+前端整合 |

> Mock 清理完成日期：2026-02-09  
> 詳細審計報告：[meetchi_dashboard_audit.md](file:///C:/Users/User/.gemini/antigravity/brain/f2ffa7b7-710d-40f9-9228-ad77e4409174/meetchi_dashboard_audit.md)

---

## 六、Consult 顧問報告索引

| 日期 | 報告 | 主題 |
|------|------|------|
| 2026-03-09 | `consult_frontend_ux_20260309.md` | 前端 UX 分析（錄音入口、主題客製化、重新設計評估） |
| 2026-03-09 | `consult_frontend_refactor_plan_20260309.md` | 前端漸進式重構計畫（三階段拆分矩陣 + 依賴圖） |
| 2026-03-09 | `consult_brand_color_design_20260309.md` | 品牌色彩設計指南（奇美色彩 → CSS Variables + Dark Mode） |
| 2026-03-09 | `audit_dev_tasks_20260309.md` | 開發任務完成度審計（標記不一致修正） |
| 2026-03-12 | `consult_meeting_tag_labels_20260312.md` | 會議卡片標籤系統（輔助色彩 + Outlook-like 自訂標籤） |
| 2026-03-12 | `consult_summary_templates_20260312.md` | 客製化摘要模板系統（Schema-Driven + 10 業務模板 + Gallery UI） |
| 2026-03-12 | `consult_role_aware_summarization_20260312.md` | Role-Aware 兩階段摘要（CoT 整合方案 + 12 學術論文） |
| 2026-03-12 | `consult_speaker_label_editor_20260312.md` | 說話者標籤編輯器 + 色彩分配 UI 設計 |
| 2026-03-12 | `consult_template_optimization_security_20260312.md` | 客製指令自動優化 + OWASP LLM Top 10 安全防護 |
| 2026-03-12 | `consult_phase8_unified_plan_20260312.md` | **Phase 8 統合開發計畫**（4 份報告統合 → 三階段 15 項任務） |
| 2026-03-08 | `consult_gemini_model_20260308.md` | Gemini 模型選擇分析 |
| 2026-03-08 | `consult_region_strategy_20260308.md` | Cloud Run GPU 區域部署策略 |