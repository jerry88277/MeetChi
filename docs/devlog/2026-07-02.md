# Devlog 2026-07-02

## 批次 4：Onboarding 冷啟動 — P2 補完（CS-8 / CS-10）並上線

延續 2026-07-01 批次 4（見 `2026-07-01.md`），本次補完當時暫緩的兩項 P2，
使批次 4 全數收斂。來源稽核：`docs/audits/2026-06-30-coldstart-onboarding-ux-audit.md`。

### CS-8（P2）擴充導覽涵蓋範圍
稽核痛點：導覽未涵蓋模板、設定、錄音、語言、機密旗標，跳過導覽者無從得知。
- `TourOverlay.tsx`：`STEPS` 由 5 步擴充為 7 步——
  - 新增純文字（center）步驟「上傳時可以這樣設定」，一次說明上傳設定視窗內的
    **範本 / 語言（國語·台語混合）/ 機密會議旗標**（這些選項在 modal 內、導覽期間未開，故不用 spotlight）。
  - 新增 spotlight 步驟 `nav-templates`（模板管理）與 `nav-settings`（系統設定），皆帶 `fallbackHint`。
- `Sidebar.tsx`：`systemItems` 的模板管理 / 系統設定加 `tourId`（`nav-templates` / `nav-settings`），
  render 補 `data-tour={item.tourId}`，讓上述 spotlight 有目標。型別採 `string | undefined`（比照 workspaceItems subtitle 模式），admin 項給 `undefined`。

### CS-10（P2）首次登入強制切 dashboard 再開導覽
稽核痛點：導覽只在 dashboard view 有 spotlight 目標；使用者若以 `?view=templates` 等落在其他分頁，
導覽開了卻空指。
- `dashboard/page.tsx`：auto-open 導覽的 `useEffect` 內，`!done && !dismissed` 時先 `setCurrentView('dashboard')`
  再 `setTourOpen(true)`，確保 `meetings-grid` / `upload-cta` 等 spotlight 目標存在。
  （nav-rag / nav-templates / nav-settings 位於側欄，任何 view 皆恆存在，與此互補。）

### 驗證狀態（物理證據）
- ✅ `npx tsc --noEmit`：0 錯誤。
- ✅ `npm run build`：Compiled successfully in 71s，8/8 靜態頁生成；本地 build 產物 chunk 含新導覽字串
  「上傳時可以這樣設定」，且無 `localhost:8000`（NEXT_PUBLIC_API_URL 正確 baked）。
- 🚀 部署 frontend（`gcloud run deploy --source`）：新 revision `meetchi-frontend-00074-rd2`（健康 True）。
  - **⚠️ 第五次流量鎖定陷阱**：deploy 後流量仍鎖在舊 `00073-6s9`（service traffic 有 tag pin：
    ux-audit-v2 / chimemo-fix / tpl-ux，導致新部署不自動切 LATEST）。
    手動 `gcloud run services update-traffic meetchi-frontend --to-revisions meetchi-frontend-00074-rd2=100` 才上線。
  - 冒煙：`/` 200、`/api/health` 200、backend `/health` 200。
  - 一致性：`/dashboard` 為 307 導向 `/login`（auth 保護，故 curl 無法直取導覽 client chunk）；
    改驗登入頁 chunk `3087e517…js` 仍含批次 4 CS-3 字串「AI 自動轉成文字」→ 證明新 revision 忠實服務批次 4+ 程式。

## 環境備忘：gcloud「Regional Access Boundary」雜訊

- 現象：Cloud Shell（devshell）環境下，每次 gcloud 指令都先噴
  `Regional Access Boundary HTTP request failed ... Account not found for email: 6f70026545|jerry_tai@mail.chimei.com.tw (404)`，
  之後仍 fallback 成功。
- 根因：組織層級 Regional Access Boundary（資料落地）強制 + devshell 代理憑證把帳號格式化為
  `<clientId>|<email>`，RAB 解析端點不認得而回 404。**非致命**——list / describe / deploy / update-traffic 皆正常。
- `regional/endpoint_mode` 未設、無 env override，故非 endpoint mode 造成。
- 真正消除：需互動式 `gcloud auth login`（瀏覽器）換掉 devshell 代理憑證為第一方使用者憑證；
  無頭工具無法觸發，須在 Cloud Shell 終端手動執行。目前不影響部署，暫維持現狀。

## 里程碑
UX 稽核優化批次 1–4 **全數項目**完成（含各批次 P2）；批次 4 無殘留暫緩項。
後續僅剩各批次刻意暫緩之架構級項目（RAG SSE 串流、模板 IA 調整等）。

## 時間
- UTC：2026-07-02 00:4x
- UTC+8（台北）：2026-07-02 08:4x
