# Devlog 2026-07-06（時間均為 UTC+8 台北）

## 上傳失敗根因分析（第一性原理 + MECE）

使用者上傳 ~120MB「數位轉型交流會」音檔，連兩次瀏覽器分段上傳都在 40–60%（chunk 6–9）附近中斷。

### 根因（兩層疊加）
- **根因①（觸發）**：企業網路封鎖直連 `storage.googleapis.com` → 前端三層 fallback（resumable 直連 → signed PUT 直連 → backend proxy chunk）被迫走最爛的第三層 backend-proxy，位元組繞經 Cloud Run，曝險加倍。
- **根因②（致命放大）**：`api.ts` 的 `uploadChunkOnce` 的 XHR **沒有設 `xhr.timeout`**。企業透明 Proxy 常「hold 住連線但不回應」→ XHR 既不 onload 也不 onerror → Promise 永久 pending → 重試邏輯不會觸發 → 整個上傳靜默凍結在 ~47%，伺服器端零錯誤、會議留下孤兒 PENDING。

### MECE 優化清單（依位元組路徑分層）
A. 客戶端邏輯：XHR timeout+ontimeout、加長退避+抖動、失敗 chunk 不牽連整體、stalled 提示。
B. 網路/Proxy：自訂網域反代 GCS 讓 resumable 直連成為主路徑；縮小 chunk（8→2–4MB）。
C. 協定/續傳：利用已存在的 `parts/NNNNNN` 做「只補缺塊」續傳；跨策略共用進度。
D. 後端合成：streaming 寫入；compose 前先驗證 parts 齊全（缺塊回明確清單而非 500）。
E. GCS：parts lifecycle 自動清理；chunk CRC32C 校驗。
F. 控制面：孤兒 PENDING TTL 掃描清理；enqueue 前再驗 GCS 物件存在。
G. 觀測/UX：upload_id 貫穿前後端；`stream_audio` 對 404 回乾淨 409/425 而非拋例外汙染 log。

（P0 止血：根因②的 XHR timeout；後端 compose 前驗證 + 前端只補缺塊續傳。待實作。）

---

## 四項 UX 優化（本日實作 + 部署）

Commit `efa64ad`；部署 `meetchi-frontend-00080-hkt`（100% LATEST，root 200 / login 200 / dashboard 307）。
本地驗證：`tsc --noEmit` 乾淨、`eslint` 0 errors、`next build` 成功。

### #1 等待焦慮 — 轉錄卡住 watchdog（DetailView）
- 問題：使用者被告知 ETA，但超時後畫面仍停在「處理中/排隊中」轉圈，無任何下一步指引（非失敗畫面）。根因是 `useMeetingPolling` 與 processing/pending UI **完全沒有逾時偵測**，會永遠輪詢+轉圈。
- 修法：加只在 pending/processing 運作的計時器，依音檔長度估算預期時間：
  - `pending` 逾 5 分 → 「似乎卡住了，可能上傳未完成」+ 重新整理／立即回報 + 提示重新上傳。
  - `processing` 逾預期 → 軟提示「比預期久，仍在處理中，**不需重新上傳**」。
  - `processing` 逾預期 ×2 → 「可能卡住了，可能網路不穩」+ 重新整理／重新從頭轉錄／立即回報。

### #2 必填欄位醒目提示（FeedbackModal）
- 問題：送出鈕在未填時 disabled，使用者不知道**缺哪一項**。
- 修法：啟用按鈕；點擊若無效 → 缺漏必填欄位（問題類型／描述／嚴重程度）轉紅框+提示，並 toast 列出缺項。欄位填妥後紅框自動消失。

### #3 前端輸入延遲（每字慢 0.5–1s）
- 調查（explore agent）：FeedbackModal 是**未 memo 的內聯子元件**，位於龐大的 dashboard page 樹中，隨 dashboard 每次重繪（10s 輪詢等）連帶重繪；且全域 console hook 為主執行緒稅。
- 修法：`React.memo` + `createPortal` 到 `document.body` 隔離 modal；`page.tsx` 以 `useCallback` 穩定 `onClose`；`DashboardView` 搜尋過濾改 `useMemo`。
- 後續建議：console hook 改為僅在錯誤路徑收集（降低全域 main-thread 稅）；MeetingCard `React.memo`；搜尋 debounce。

### #4 說話者標籤 Enter 存檔（DetailView）
- 問題：名稱／角色輸入框無鍵盤處理，只能點勾勾存檔。
- 修法：兩個輸入框加 `onKeyDown`，Enter → `handleSaveSpeaker`、Escape → 取消。

---

## 三項新功能：降噪 / 逐段改說話者 / 摘要同步（本日實作）

依第一性原理與 MECE 拆解使用者情境（VDI 會議室、喇叭播放→筆電內建麥克風錄音，機型 ASUS ExpertBook B1402CVA）。

### #1 後端降噪（可行性結論 + 保守實作，flag-gated，**暫不部署 GPU**）
- 可行性：✅ 可行但須**保守**。研究結論：喇叭→麥克風情境的殘響/回聲比雜訊更傷 ASR；**輕度**降噪（highpass 去低頻嗡聲 + 溫和 `afftdn` + `loudnorm` 響度正規化）在低訊噪比時有助 ASR 且保留語音資訊；**重度**降噪反而吃掉子音、產生偽影，降低 Whisper 準確率。
- 實作：`apps/backend/gpu_service/main.py` 新增 `_denoise_audio()`，於音檔下載後、轉錄前插入（`_run_asr_processing`）。以 `ENABLE_DENOISE`（預設 false）開關，濾鏈可由 `DENOISE_AF` 覆寫以利 A/B。ffmpeg 失敗自動回退原檔，確保不劣化。
- **決策**：依使用者指示先實作、flag 關閉、**先不重新部署 GPU**（GPU 成本敏感，見 checkpoint 001）。底部播放器改放降噪後音檔一併等 GPU 部署時處理。

### #2 逐段重指派說話者（per-segment speaker reassignment）
- 問題：ASR 把不同說話者的話錯歸為同一人；聚合視圖無法單獨修正。
- 後端：`PATCH /api/v1/meetings/{id}/segments/speakers`，body `{ updates: { segment_id: 新原始標籤 } }`，更新 `TranscriptSegment.speaker`。
- 前端：`transform.ts` 新增未聚合 `rawSegments`（帶 id）；DetailView 逐字稿區新增「逐段編輯說話者」模式，每段下拉選擇既有說話者，追蹤待存變更，儲存後重新載入取得最新聚合。

### #3 摘要同步說話者（hybrid：LLM 快掃重貼 + 建議重生）
- 問題：更新說話者後摘要未同步。
- 後端：`POST /api/v1/meetings/{id}/resync-summary-speakers`。`llm_utils.relabel_summary_speakers()` 用 Gemini **僅修正摘要中的說話者名稱引用**（保留其餘內容與 JSON 結構，頂層鍵不一致則拒絕採用）；並依啟發式（逐字稿實際說話者集合 vs 摘要 `speaker_contributions` 引用集合）判斷是否**建議整份重生**。變更前備份 `SummaryVersion`。
- 前端：DetailView 新增「更新摘要（同步說話者）」鈕；結果面板顯示已同步/無需修改/建議重生，並提供「重新整理查看」與「整份重生摘要」動作。

本地驗證：後端 `ast` + 模組匯入（新路由註冊成功）；前端 `tsc --noEmit` 乾淨、`next build` 成功。

---

## 💰 週末費用追蹤調查 — Backend minScale 回歸問題

### 問題

7/4-7/5（週六日）帳單仍有 Cloud Run 費用產生。GPU ASR 已於 6/22 設定 minScale=0，預期週末 $0。

### 調查結果

**GPU ASR: minScale=0 正確生效 ✅**
- Serving revision `meetchi-gpu-asr-00074-85q`（minScale 空 = 0）
- 7/4-7/5 零 instance startup logs → GPU 費用 $0

**Backend: minScale 被覆蓋回 1 ❌**
- 6/22 設定的 minScale=0（revision phaseb-v2）在 **6/23 18:20 (UTC+8)** 被另一次部署覆蓋
- 原因：`.agent/skills/gcp-cloud-run/SKILL.md` hardcode `--min-instances=1`
- AI agent 依該 skill 部署時自動重設 minScale=1
- 加上 4 個殭屍 tagged revisions（audit-fix, audit-fix-v2, smtp-chimei, tpl-auth）各自 minScale=1

**費用組成（$10-25/day）：**
| 資源 | 日成本 |
|------|--------|
| Backend 主 instance (minScale=1, always-allocated) | $5.01 |
| Backend 殭屍 tags ×4 | $5-20 |
| Cloud SQL | $0.33 |
| GPU ASR | $0 |

### 執行修復

1. ✅ 移除 backend 殭屍 tags（audit-fix, audit-fix-v2, smtp-chimei, tpl-auth）
2. ✅ Backend minScale=0（revision meetchi-backend-00100-ftt）
3. ✅ 修改 `.agent/skills/gcp-cloud-run/SKILL.md`：`--min-instances=1` → `0`
4. ✅ 強化 `.agent/workflows/gcp-deploy.md`：新增部署後雙重 scale-to-zero 驗證
5. ✅ Health check 通過：`{"status":"healthy","service":"meetchi-backend"}`

### 後續觀察

- 觀察 7/6-7/12 帳單，預期非使用期 < $1/day（僅 Cloud SQL + Storage）
- Backend cold start 預估 2-5 秒（遠低於 GPU ASR 的 45-60 秒）

---

## #1 降噪部署（GPU）+ 降噪音檔播放持久化

依使用者確認「可以重新部署，確保不會有殭屍實例」+「詳情頁底部播放器放降噪後音檔」，本次一次性完成（避免二次 GPU 重建成本）：

- **GPU 影像重建**：`cloudbuild-gpu.yaml` → `meetchi-gpu-asr@sha256:23661c2f...`。
- **部署**：`gcloud run deploy meetchi-gpu-asr --image <digest> --min-instances=0 --update-env-vars ENABLE_DENOISE=true` → 新 revision `meetchi-gpu-asr-00075-xfj`，`update-traffic --to-latest` → 100% LATEST。
- **殭屍實例防治**：`minScale` 未設（=0）→ 閒置自動縮到零，不留常駐 GPU 實例計費。`/health` 200（新影像 + 降噪啟用開機正常）。
- **降噪音檔播放持久化**（bundled，非致命）：
  - GPU `_make_and_upload_playback_denoise()`：以原始品質套用相同輕度濾鏈 → AAC/m4a，上傳到與原檔同目錄 `denoised.m4a`。
  - 後端 `GET /{meeting_id}/audio-url`：偵測到同目錄 `denoised.m4a` 即優先簽發播放（找不到回退原檔）。→ 底部播放器自動播放降噪後音檔。
  - 部署 `meetchi-backend-00101-shc`，`--to-latest` 100% LATEST，`/health` 200、`audio-url` 200。

> 註：降噪僅在**新的轉錄/重新轉錄**時生效；既有已完成會議需重新從頭轉錄才會產生 `denoised.m4a`。建議挑 1 筆 VDI 錄音（如「數位轉型交流會」）重新轉錄做 A/B。

## 卡住 Session 複查
- 「數位轉型交流會20260703」(`f71598b8`) 現況 **COMPLETED**（`deleted_at=None`，`updated_at` 2026-07-06 01:09）——先前卡在 ~47% 的上傳其實**已完成轉錄**，非卡住、也未被刪除。
- `72f14ad9`（同名先前一次）已不存在（404）。
- 另發現既有殭屍**資料**（非實例）：`1f7f42d0`/`64740715` PROCESSING 約 18 天、`7948f6b5`/`83c7b826`/`ea901327` PENDING 約 25 天——與本次無關，待確認是否清理。

## 降噪 A/B 驗證（f71598b8，非破壞性 /asr/refine 直呼）
以已部署 `ENABLE_DENOISE=true` 的 GPU（`meetchi-gpu-asr-00076-hsx`）直呼 `/asr/refine`（**不帶 callback_url → 不寫 DB**），對同一音檔重跑，與降噪關閉的 baseline 比較。

**GPU log 實證：**
- `[Denoise] Applied 'highpass=f=80,afftdn=nf=-20,loudnorm=I=-16:TP=-1.5:LRA=11' -> denoised.wav`（ASR 前處理生效）
- `[Denoise-Playback] Uploaded gs://.../f71598b8-..._denoised.m4a (112402246 bytes)`（原品質播放檔上傳，**採防碰撞命名 `{meeting_id}_denoised.m4a`**）
- 轉錄對象為 `denoised.wav`，時長 01:53:45。

**A/B 數據：**
| 指標 | 降噪 OFF (baseline) | 降噪 ON |
|---|---|---|
| 逐字內容總字數 | 32,862 | 32,664（−0.6%）|
| 片段數 | 862（已聚合入庫）| 1,993（refine 原始未聚合，階段不同，不可直接比）|

**結論：**
- **內容保真**：降噪後逐字內容字數僅差 0.6%，會議資訊完整保留，未因降噪吞字——符合「保守降噪 + 保留會議資訊」目標。
- 降噪僅作用於新／重跑轉錄，不影響既有已完成會議。
- `/asr/refine` 同步回傳不含 diarization 講者標記（`speakers_count=0`），故講者數 A/B 無法由此路徑比較（完整 callback 路徑才會做講者指派）。

**播放端驗證：**
- GCS 現有兩檔：`..._.m4a`（119MB 原始）+ `..._denoised.m4a`（112MB 降噪）。
- 後端 `GET /api/v1/meetings/{id}/audio-url` 回傳的簽章 URL **已指向 `..._denoised.m4a`**（`response-content-type=audio/mp4`），即詳情頁底部播放器將播放降噪版音檔。✅

**成本：** GPU `minScale` 空值（=0，跑完自動縮回零），無殘留實例。

## 卡住 BUG 確認與修復（會議 395a3f03「錄製 (7)」）
使用者反映會議 `395a3f03-8059-4d1c-9a6a-c0ebcc54c957` 疑似卡住，經確認為**兩個**串連的卡住 bug。

### 診斷
- 上傳成功（77.78MB，`00:04:32`），任務於 `00:04:34` 被 claim 轉 PROCESSING、開始 CORE 處理後 **worker 實例死亡/逾時**，之後 3h+ 無進展。
- GPU 服務全無此會議日誌、`transcript_segments=0`、`failure_reason=None`、狀態凍結在 `PROCESSING`。
- 根因（**Bug A**）：worker claim 後死亡，資料列鎖在 PROCESSING；重試只認 `PENDING/FAILED` 故 Cloud Tasks 重試一律 skip → **無法自我復原**（與既有殭屍 `1f7f42d0`/`64740715` 同類）。

### Bug B（復原時新發現）
重新處理後轉錄完成但 **6 個 chunk 全部 Empty result → 0 段落**。
- 音檔健檢實證（ffmpeg volumedetect，84 分鐘全長）：`mean_volume −91.0 dB`、`max_volume −78.3 dB` → **音源近乎數位靜音**（麥克風未收到訊號）。`audio_stats.health="silent"` 系統本已正確判定。
- **排除降噪疑慮**：靜音來自來源音檔本身，非降噪造成。
- 但空逐字稿分支 `tasks.py` 直接 `return {"status":"skipped"}`，呼叫端 `handle_transcription_task` 把非 `completed/accepted` 視為 **HTTP 500**，且會議停在 `TRANSCRIBED/summarizing` 非終態 → Cloud Tasks 重試又因狀態非 PENDING/FAILED 被 skip → **二次永久卡住**。

### 修復
1. **`admin.py` 新增自助復原端點**（Bug A 通用解）：
   - `GET /api/v1/admin/stuck-meetings?min_stuck_minutes=` — 列出停滯的 PROCESSING/REFINING/TRANSCRIBED 會議。
   - `POST /api/v1/admin/reset-stuck-meeting?meeting_id=&min_stuck_minutes=&reenqueue=&force=` — 安全把卡住列重置回 PENDING（清 processing_stage/failure_reason）並重新 enqueue。安全閘門：拒絕 COMPLETED、預設要求停滯達門檻（可 `force`）。
2. **`tasks.py` 空逐字稿分支改為落終態**（Bug B）：靜音/無訊號 → `status=COMPLETED`、`processing_stage=None`、`failure_reason` 取 `audio_stats.health_label_zh`（使用者可讀），回傳 `completed` 避免 500 與重試卡住。

### 驗證（實測）
- reset 端點成功把 `395a3f03` 由 `PROCESSING`→`PENDING` 並重新 enqueue，轉錄管線完整跑到底（不再凍結）。
- 修復後重跑，會議最終 `status=COMPLETED`、`stage=None`、`failure_reason=「未偵測到可辨識語音（可能麥克風未開啟、權限被擋，或錄到無聲音源）」`。log：`finalized COMPLETED with empty transcript (silent audio)`。
- `stuck-meetings` 端點另列出既有兩筆殭屍 `64740715`(AWS-INFA)、`1f7f42d0`(美玉)（~18 天），可用同端點清理。

**部署**：backend `00103-pbz`（reset 端點）→ `00104-78n`（含空逐字稿修復），皆 100% LATEST。

## 復原功能權限強化 + Ops 面板 UI（安全缺口修復）
使用者提問促使複查：復原端點是否為管理者專屬、應在何處使用。發現**安全缺口**並修復。

### 發現的問題
- 初版 `reset-stuck` 放在 `admin.py`，僅靠軟性 `ADMIN_TOKEN` header 把關，而該 env var **未設定** + backend ingress=all → **任何人可呼叫**，不符「管理者專屬」預期。
- 專案已有正確角色權限模型：`ops_admin.py` 的 `require_admin`（`User.role ∈ {admin, super_admin}`）與前端 `OpsAdminPanel`（僅 admin 可見）。

### 修復
1. **後端**：把 `reset-stuck` 改實作於 `ops_admin.py`，改用 `require_admin` 角色閘門：
   - `POST /api/v1/ops/meetings/{id}/reset-stuck?force=&reenqueue=`（重置卡住會議 → PENDING + 重新 enqueue）。
   - `GET /api/v1/ops/meetings` 回應新增 `processing_stage`/`stuck_minutes`/`is_stuck`（停滯門檻 15 分鐘），供 UI 標示。
   - **移除** `admin.py` 內未受保護的 `reset-stuck-meeting`/`stuck-meetings` 端點（關閉後門，改 404）。
2. **前端**：`OpsAdminPanel` 會議分頁新增「操作」欄；PROCESSING/REFINING/TRANSCRIBED 顯示按鈕，`is_stuck` 者以紅色「復原卡住」醒目標示（附停滯分鐘 tooltip），點擊 confirm 後呼叫 reset 並重載。

### 使用位置（回答使用者 Q2）
管理者登入 → 儀表板「系統維運管理」(Ops) 面板 → 會議分頁 → 對卡住會議點「復原卡住」。不需再手動 curl/gcloud。

### 驗證（實測，UTC+8）
- backend `00105-7zr`、frontend `00082-wr4`，皆 100% LATEST，`/health` 200。
- 舊 ungated `POST /api/v1/admin/reset-stuck-meeting` → **404**（後門已移除）。
- 新 `POST /api/v1/ops/meetings/{id}/reset-stuck` 對 COMPLETED 的 395a3f03 回 **409**（狀態守衛正確）。
- `GET /api/v1/ops/meetings` 正確標記 2 筆殭屍 `1f7f42d0`(美玉)、`64740715`(AWS-INFA) 為 `is_stuck=true`（停滯 ~26070 分鐘≈18 天）；395a3f03 為 COMPLETED/is_stuck=false。

### 權限落實補充（誠實揭露）
- 目前 backend `AUTH_REQUIRED` 未設（預設 false，dev-mode 略過真實驗證），此為**全站既有姿態**，`require_admin` 對「所有」Ops 端點皆為軟性。要硬性落實角色驗證需設 `AUTH_REQUIRED=true` + 真實 IdP，屬影響全站的基礎設施決策，應循 `terraform/` 變更（agents.md §3），不以 `gcloud --set-*` 暴力覆蓋。此次已把 reset 收斂到與其他 admin 功能一致的正確權限機制，待全站啟用即同步生效。

### Q3 回答
會議 `395a3f03`「錄製 (7)」音檔確為**靜音**：ffmpeg 全長 84 分鐘實測 `mean −91.0 dB / max −78.3 dB`，`audio_stats.health="silent"`。

## Ops 面板「復原卡住」按鈕可見性修正（frontend-00083-hrh）
- 回報：管理者在「系統維運 → 會議明細」看不到恢復按鈕，操作欄全為「—」。
- 查證：後端/CORS/權限/流量/部署 bundle 全數正常；`/ops/meetings` 正確回傳 `is_stuck`。
  根因為**可見性**：2 筆真正卡住的會議（美玉、AWS-INFA，停滯 18 天）在 created_at DESC
  排序下位於第 12、14 列，被上方 COMPLETED（顯示「—」）列淹沒，需捲動才看得到。
- 修正（OpsAdminPanel.tsx）：
  1. 卡住會議（is_stuck）置頂排序，按鈕一眼可見。
  2. 表格上方新增紅色警示橫幅：顯示卡住筆數與操作指引。
  3. 卡住列以淡紅底色標示。
- 驗證：tsc 0 error；部署 00083-hrh 100% LATEST；拉取線上 image grep 確認 banner 字串存在。

## 復原按鈕仍不顯示 — 根因：狀態大小寫不一致（frontend-00084-rtp）
- 現象：紅色橫幅/置頂/紅底都出現（皆基於布林 is_stuck），但操作欄仍為「—」，按鈕不顯示。
- 第一性原理 + MECE 拆解：橫幅走 is_stuck（布林、無大小寫問題）；按鈕走
  `['PROCESSING','REFINING','TRANSCRIBED'].includes(m.status)`（字串、區分大小寫）。
  兩者資料來源不同 → 只可能是 status 值不符預期。
- 用 Playwright 以 UAT 帳號實際登入重現：API 回傳 status="PROCESSING"，但元件內
  m.status 顯示為 "processing"（小寫）。
- 根因：`api.ts` fetch 有 **L1 Boundary Normalization**（lines 273-284），會把所有
  回應的 status 欄位一律轉小寫（歷史上造成過 3 次事故的統一慣例）。我的按鈕條件用大寫
  陣列，永遠 match 不到小寫 status → 恆走 else 顯示「—」。同一根因也讓 statusBadge
  （大寫 key）全部 fallback 成灰色。
- 修正（OpsAdminPanel.tsx）：
  1. 按鈕條件改為小寫並防禦性 toLowerCase：`['processing','refining','transcribed'].includes((m.status||'').toLowerCase())`
  2. statusBadge 色彩 key 全改小寫並補齊 refining/transcribed/recording，顯示以 CSS uppercase 呈現。
  3. 復原確認對話框顯示狀態改為大寫。
- 驗證（實體證據）：Playwright 實登重現 → 復原卡住按鈕數 = 2、前兩列（美玉/AWS-INFA）
  status=PROCESSING 且操作欄=「復原卡住」；截圖確認橫幅、藍色 PROCESSING badge、紅色按鈕皆正常。
  tsc 0 error；部署 00084-rtp 100% LATEST。

## 前端效能優化 A+B+C（frontend-00085-kr5）
針對回報模組打字延遲（VDI/CPU x4 下 p75 ≈ 424ms，為 dashboard 搜尋 baseline 32ms 的 8–15 倍），
依 frontend-perf-report 的根因分析實作三項改善並一次部署：

- **A. 移除 FeedbackModal 全螢幕 backdrop-blur**：overlay `bg-black/50 backdrop-blur-sm` → `bg-black/60`。
  全螢幕毛玻璃會在每次按鍵（畫面更新）重新對整個 viewport 做模糊，成本與 CPU 節流無關（約 120ms/鍵）。
- **B. 記憶化選項格（icon-heavy grids）**：將 issueType/severity/frequency 三組 lucide 圖示格抽成
  module-scope `React.memo` 元件（IssueTypeGrid / SeverityGrid / FrequencyGrid）。打字時這三個值不變，
  useState setter 為穩定參考，memo 子元件跳過重繪，避免整個 33KB modal 隨每次按鍵重繪（約 150–280ms）。
- **C. 路由層 code-split（next/dynamic）**：dashboard/page.tsx 把非首屏、條件渲染的重元件
  （DetailView / RecordingView / SettingsView / TemplateGallery / RagWorkspace / RagDrawer /
  UploadSettingsModal / FeedbackModal / AdminFeedbackPanel / OpsAdminPanel / TourOverlay）改為
  動態載入（ssr:false），移出主 bundle。build 後由單一巨型 bundle 拆為 30 個 chunk。
  首屏保留靜態：Sidebar / UATBanner / DashboardView / UploadTray / ConfirmDialog。

### 驗證（實體證據，Playwright + CDP CPU x4，生產 00085-kr5）
| 指標 (CPU x4) | Before | After (2 runs) |
|---|---|---|
| feedback-summary 打字 p75 延遲 | ~424ms | **56 / 64ms** |
| feedback-summary 單鍵 maxDur | 明顯偏高 | 120 / 72ms |
| dashboard-search baseline p75 | 32ms | 40 / 32ms |

回報模組打字延遲由 baseline 的 8–15 倍降至約 1.5–2 倍（≈6–7× 改善）。
tsc 0 error；`next build` 成功、拆為 30 chunk；部署 00085-kr5 100% LATEST，prod /login 200。

## 會議詳情頁 UX 改善（frontend-00086-dwc）
針對用戶在審閱逐字稿時的兩個痛點進行改善：

### 1️⃣ 專有名詞浮窗化
**問題**：審閱逐字稿時，專有名詞列表與逐字稿無法同時顯示，用戶需要上下滑動。
**解決**：實作 `FloatingGlossaryPanel` 組件 — 可拖拉、可縮放的浮窗
  - ✓ 任意位置拖拉（使用 mouse events + refs 手動實作）
  - ✓ 右下角縮放（最小 280×300px）
  - ✓ 最小化/還原
  - ✓ localStorage 記憶位置和大小
  - ✓ Portal 到 document.body（不受逐字稿滾動影響）
  - ✓ 無外部依賴（純 React + localStorage）

### 2️⃣ 說話者同步提示矛盾修復
**問題**：用戶重新生成摘要後，同時看到矛盾的消息：
  - "摘要中的說話者名稱已與最新標籤一致，無需修改"
  - "建議整份重生摘要"（檢測到說話者集合有變動）
  
**根因**：後端 `resyncSummarySpeakers` 返回的 `updated` 和 `recommend_regenerate` 是獨立信號，
可能一方為 false 另一方為 true（LLM 沒找到要修改的引用，但集合確實變動了）。

**解決**：互斥的前端顯示邏輯（優先級）
  1. error → 只顯示錯誤
  2. updated=true → 只顯示成功（不顯示建議）
  3. recommend_regenerate=true → 只顯示建議重生的理由
  4. 其他 → 顯示"無需修改"

使用者現在能清楚理解下一步（無需操作、重新整理或整份重生）。

### 驗證
- tsc：0 errors
- build：成功、30 chunks
- deploy：frontend-00086-dwc 100% traffic
- health check：200 OK
- commit：0c3fbb2（推送 origin + enterprise）

---

## 2026-07-07 13:xx (UTC+8)｜迴歸修復：浮窗遮住「重新生成摘要」選單

### 問題（使用者回報）
會議詳情頁右上角的「重新生成摘要 / 換模板 / 匯出」選單消失。

### 根因（第一性原理 + MECE）
非選單被刪除，而是被**浮動詞彙表面板遮住**：
- `FloatingGlossaryPanel` `defaultOpen=true`，載入即自動開啟
- 預設位置 `x = innerWidth - 320, y = 60`（螢幕**右上角**）
- `position: fixed`、`zIndex: 50`、portal 渲染至 `document.body`
- 正好覆蓋 header 右上角的 regenerate/template/export 控制列

### 修復（兩層防護，MECE）
1. `glossaryPanelOpen` 預設改為 `false` — 載入時不自動開啟，不遮任何控制；
   使用者按逐字稿區塊「顯示詞彙」按鈕才開啟。
2. `defaultPosition` 下移至 `y=180`（header 控制列下方）— 即使開啟也避開選單。

未更動重新生成摘要邏輯本身；header 選單程式碼零改動。

### 驗證
- tsc：0 errors
- build：成功
- deploy：frontend-00089-58g 100% traffic
- health check：healthy（台北 2026-07-07 13:16:25）
- commit：3e3ab7c（推送 origin + enterprise）

### 教訓
新增 portal/fixed/高 zIndex 的浮動元件時，預設位置必須避開既有固定控制列；
或預設關閉，交由使用者主動開啟，避免遮蔽既有功能（迴歸）。

---

## 2026-07-07 14:xx (UTC+8)｜#2 套用修正即時顯示 + #1 模板系統盤點

### #2 套用專有名詞修正後免重整（已實作上線）
根因：`apply_glossary_correction` 改 DB 的 TranscriptSegment 並 commit，但 API 只回
`segments_corrected` 數字，前端 meeting.transcript 是舊的 → 需手動重整。
修法：樂觀覆蓋層。FloatingGlossaryPanel 新增 onCorrectionApplied 回調；DetailView
以 glossaryCorrections state + applyGlossaryOverlay() 於 render 時套用相同的
wrong→correct 字串替換（與後端等價、idempotent），切換會議時清空避免重複套用。
deploy：frontend-00091-6gj，health healthy（台北 14:38）。commit 3985a7e。

### #1 模板系統盤點（分析，待決定實作範圍）
**核心發現：模板選擇目前對輸出「幾乎無效果」，training 與 general 產出相同。**

兩個獨立缺陷（MECE）：
- **缺陷 A｜後端長會議忽略模板**：generate_summary() 在 transcript ≥ 15,000 字
  （MULTI_PASS_THRESHOLD）走 multi_pass_summary.py，該路徑用**寫死的 prompt**
  產生 chapters + 固定 meta（tldr/decisions/risks/action_items/…），
  template_name 有傳入但完全沒用。多數真實會議都 ≥15K → 模板零作用。
- **缺陷 B｜前端渲染忽略模板**：DetailView 只渲染固定版面（summary +
  decisions/risks/action_items + chapters），從不讀 template.sections、
  不渲染自訂 output_key（key_learnings/qa_summary/BANT/STAR_stories/ideas…）。
  即使後端有產生也看不到。

次要問題：
- TEMPLATE_KEY_MAP 只認得 4 個模板；其餘 6 個系統模板與所有自訂模板原樣穿透。
- 單次路徑（<15K）走 build_prompt_from_template，但 get_template_by_name 只查
  SYSTEM_TEMPLATES → **自訂模板一律 fallback 成 general**。
- V2 requirements 無論如何都注入，固定欄位主導輸出。

結論：模板系統目前近乎「裝飾性」。優化計畫見下方（分階段），待使用者決定範圍。

---

## 2026-07-07 15:xx (UTC+8)｜#1 模板系統真正生效（策略 a，Phase 1-4）

### 問題
換模板（教育訓練）重生後，輸出與一般會議一模一樣。兩個獨立缺陷：
- 後端：長會議（≥15K）走 multi-pass，用寫死 prompt，完全忽略模板；自訂模板 fallback 成 general。
- 前端：DetailView 只渲染固定版面，不渲染模板自訂 output_key。

### 策略 (a)：V2 核心通用 + 模板專屬加值層
後端：
- template_engine：UNIVERSAL_OUTPUT_KEYS / get_template_specific_sections /
  template_from_db_row（解析自訂模板）。
- multi_pass_summary：新增 fail-safe Pass 2b，依章節脈絡生成模板專屬 output_key，
  併入結果但不覆蓋 V2。
- llm_utils.generate_summary：新增 template_obj 參數並轉傳 multi-pass。
- tasks.py：解析系統或 DB 自訂模板後傳入。
- meetings.py：regenerate-summary 回寫 meeting.template_name（修既有 bug，
  讓詳情頁 output_key→title 對照正確）。
前端：
- transform.ts：擷取非 V2 欄位到 meeting.extraSections。
- detail/TemplateSections.tsx：動態渲染 string/list/object[]/object；標題取自
  會議模板，並內建常見 output_key 中文 fallback 對照。
- DetailView：V2 核心區塊後渲染 TemplateSections；模板下拉加說明（Phase 1 誠實化）。

### 端到端驗證（物理證據）
- 重生 f71598b8（training）→ summary_json 新增 key_learnings(16)/qa_summary(16)/
  further_reading(17)，V2 欄位（chapters/decisions/risks）仍在；general 無專屬欄位。
- template_name 寫回：POST 後同步變 training（PROCESSING）→ 完成後 COMPLETED|training。
- tsc 0 / next build OK / 13 後端測試通過。
- 部署 backend-00107-c8f、frontend-00093-fq7，兩者 /health healthy。commit 7ca17d2。

### 附記
Cloud Shell /home 期間一度用罄（source deploy 暫存）；清理 /tmp 殘留 installer(1.4G)
+ ~/.cache/uv + .next/cache 後恢復。
