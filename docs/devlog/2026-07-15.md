# Devlog 2026-07-15

## 線#1 落地：pre-Whisper 頻譜 Gate（開頭多人交談幻覺抑制）

承接 2026-07-10 研究（`docs/research/uat03_hallucination_analysis/`，Gate 設計已定案、
待實作進 GPU pipeline）。本次把該 Gate 從研究落地為生產程式並部署驗證。

### 實作
- **新增 `apps/backend/app/spectral_gate.py`**（純 numpy.fft，community-1 環境無 scipy）：
  - 逐窗特徵（win 1.5s / hop 0.5s）：頻譜傾斜 `10·log10(E[2–4kHz]/E[<750Hz])`、
    低頻 CPP（倒頻譜峰突起，harmonic clarity）、高頻占比 `E[2–4kHz]/E_total`。
  - `score = robust_z(tilt) + robust_z(cpp)`，每場以 **median/MAD** 自適應正規化（抗房間/麥克風漂移）。
  - **full 模式（預設）**：全檔遲滯，把持續「低分且悶」的區段遮成靜音（mask-not-delete，
    時間軸不變），交 faster-whisper VAD 跳過。**絕對高頻護欄**（hf≥0.06 一律不遮）保護真實語音；
    另有最大遮罩比例上限與最短遮罩持續護欄。備 leading 模式但實測會被錄音起始高頻瞬態誤判，故非預設。
- **整合** `offline_asr_community1.py::_transcribe_sync`：`ENABLE_SPECTRAL_GATE` 開啟時，
  進 Whisper 前 `whisperx.load_audio → apply_gate → 傳 masked numpy 給 CT2`（免重編碼），失敗回退原檔。
- **Dockerfile.gpu-service-community1** 補 `COPY app/spectral_gate.py`（否則 import 失敗、gate 靜默失效）。
- **測試** `tests/test_spectral_gate.py` 6 案全過；後端全套 240 passed（234+6），無回歸。

### 本機驗證（uat03 前 12 分，7:52=472s 為幻覺真值邊界）
- full 模式遮 **40.6%**，其中 **290s 落在 0–472s 幻覺區、僅 2s 落在真實區（472–720s）**。
- 幻覺區訊號特徵印證研究：score 負、tilt −15~−23dB、hf 0.005–0.028；真實區 score +3~+4.5、hf 升至 0.11。

### 部署
- GPU build `cloudbuild-community1.yaml` → image `community1-sgate-0715`（build `c83fbfcd` SUCCESS）。
- `gcloud run deploy meetchi-gpu-asr`，`ENABLE_SPECTRAL_GATE=true`。最終生產 revision **`meetchi-gpu-asr-00084-4qq`**。

### 生產 A/B（同一 chunk_000＝uat03 前 900s，僅切換 flag，deployed 程式一致）
| 區間 | Gate OFF（baseline） | Gate ON | 效果 |
|---|--:|--:|---|
| 開頭 0–472s（幻覺區） | **175 段** | **33 段** | **−81%** |
| 真實 472–900s（業務內容） | 140 段 | 144 段 | **保留（未劣化）** |

- baseline 開頭為典型會前 murmur 幻覺：`哇 / 這兩盞車 / 我來搞方啦 / 孤字「我」「你」/ 做我覺得要蘋果`。
- 真實區內容兩者皆完整連貫：`因為產業屬性的關係我們現在的水股就越來越大 / …指標7.0 / 蠻常被政府單位關注 / 所以我們今天想提出一個解方`。

### 誠實結論
- Gate 把開頭幻覺段數砍 81%，**且不損真實語音**（絕對高頻護欄有效）。
- 非 100% 消除：殘留 33 段中含少量真實會前對話與零星幻覺 loop（遮罩間隙讓 Whisper 仍能拼段）。
  屬「訊號層 Gate + 既有參數抑制」疊加後的合理殘量，較 baseline 為大幅改善。
- flag-gated，回滾＝`ENABLE_SPECTRAL_GATE=false`。
- 報告：`docs/reports/spectral-gate-ab-20260715.md`。

### 待辦
- 線#2 台語辨識（mms-lid-4017 路由 Breeze-25/26）尚未開始（依使用者指示，線#1 驗證部署後再進行）。

---

## 線#2 落地：台語辨識路由（MMS-LID → Breeze-25/26）

以 `facebook/mms-lid-4017`（Wav2Vec2, 4017 語言 ISO 639-3）逐段判斷語言，台語（nan）
路由 Breeze-ASR-26、其餘 Breeze-ASR-25，取代舊 Plan B 的 `avg_logprob` 低信心代理。

### 實作
- 新增 `app/lid_router.py`：lazy-load MMS-LID，整段音檔一次載入 numpy、記憶體切片、批次推論；
  回傳每段 top_lang/top_prob/nan_prob/top-k。
- `gpu_service/main.py::_retranscribe_taiwanese_lid`：LID 判台語者才以 Breeze-26 重轉（記憶體 clip）。
  `TAIWANESE_ROUTING=lid|confidence`（預設 lid），僅 `zh-nan` 模式啟用。
- **踩雷**：Dockerfile 以顯式 `pip install` 逐套安裝、whisperx `--no-deps`，故 transformers 原本缺
  → 首版部署 `[LID] No module named 'transformers'`。修：Stage 4 加 `transformers>=4.30,<5` + build 期 import 斷言。
- Dockerfile 補 `COPY app/lid_router.py`；後端全套 247 passed。

### 關鍵發現與校準（實測 TaigiSpeech 台語 12 段 vs uat03 華語 157 段）
- MMS-LID 對台語的 `nan` 機率被近親漢語稀釋（台語 nan_prob mean=0.112；華語 mean=0.001）。
- 判準比較：top-1=nan → recall 1/12(8%)、FP 0/157；**nan∈top3 → recall 7/12(58%)、FP 1/157(0.6%)**。
- 故採 **top-k 成員判準**（`LID_TW_TOPK=3` 預設）；絕對機率門檻無效（稀釋）。

### 部署與驗證
- GPU build `community1-lid-0715d`，生產 revision **`meetchi-gpu-asr-00090-p66`**
  （`ENABLE_SPECTRAL_GATE=true`、`TAIWANESE_ROUTING=lid`、`LID_TW_TOPK=3`）。
- 台語 clip：**7/12 判台語 → Breeze-26 重轉 7/7**；華語 chunk：top-1 誤判 0/157、top3 1/157。
- 回滾：`TAIWANESE_ROUTING=confidence` 或 `LID_TW_TOPK=1`。
- 報告：`docs/reports/lid-taiwanese-routing-20260715.md`。

### 誠實限制
- recall 上限受 MMS-LID 台/華辨識力所限（非 100%）。可再降 topk 嚴格度（FP 上升）或換專門判別器。

---

## 線#2 優化：閉集重正規化 + 不對稱成本實驗

第一性原理分析後選定兩個「動 root cause、低成本」方案落地（使用者拍板）。

### 方案 A：閉集重正規化（closed-set renormalization）
- root cause：MMS-LID softmax 攤 4017 類，nan 機率被近親漢語稀釋。
- 解法：機率遮罩到 `{cmn,nan,eng}` 重正規化，nan 與華語一對一比。閉集模式改以 `nan_prob_cs` 路由。
- 實測：台語 `nan_prob_cs` mean **0.761**、華語 mean **0.003**（分離極大）。
  **recall 8%→58%→83%（10/12），華語 FP 0/29**。門檻 `LID_CS_NAN_PROB=0.2`（華語 max 0.027 的 7 倍餘裕）。

### 方案 B：不對稱成本實驗
- 問題：華語誤送 Breeze-26 會不會劣化？→ 實測 Breeze-26 **會把華語轉錯且過度自信**
  （`符合法規`→`組合法會`）。故 **不可無腦積極路由**；正解是高 recall+高 precision（即方案 A）。
- 旁證：閉集下華語 slice 0/29 路由，逐段文字與 Breeze-25 baseline **完全相同**（diff=0）。

### 部署
- GPU build `community1-renorm-0715`，生產 revision **`meetchi-gpu-asr-00094-fxm`**
  （`LID_ALLOWED_LANGS=cmn,nan,eng`、`LID_CS_NAN_PROB=0.2`、`TAIWANESE_ROUTING=lid`、gate on）。
- 後端全套 248 passed。回滾：清空 `LID_ALLOWED_LANGS` 回開集。
- 報告：`docs/reports/lid-closedset-asymmetric-20260715.md`。

### 踩雷
- gcloud `--update-env-vars` 以逗號分隔多個 var，`LID_ALLOWED_LANGS=cmn,nan,eng` 的逗號會被吃掉
  → 首次誤用連字號 `cmn-nan-eng` 使閉集失效。改用自訂分隔語法 `^@^LID_ALLOWED_LANGS=cmn,nan,eng` 修正。
