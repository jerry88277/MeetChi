# Devlog 2026-07-01

## 批次 2：ChiMemo（跨會議 RAG）UX 優化 — 部分完成並上線

依 `docs/audits/2026-06-30-module-chimemo-4persona-audit.md` 實作 9 項（P0×1、P1×7、P2×1）。

### 後端

#### R-A1 歷史對話還原 citations（P0，根因修復）✅
- 根因：`rag_query_logs` 只存 `answer_preview`(截斷1000字) + `citation_count`，**未存 citations 本體**，
  故載入歷史對話後引用來源全部消失。
- `app/routes/rag.py`：
  - `_log_rag_query()` 加 `citations` 參數，序列化 `citations[:10]` 存入新欄位 `citations_json`。
  - `ask` endpoint 呼叫傳入 `citations=citations`。
  - `RagHistoryItem` model 加 `citations: List[Citation]`；`/history` endpoint SELECT 加 `citations_json`，
    以 `_parse_citations()` 安全解析（舊資料 NULL → 降級空清單）。
- `app/main.py`：startup safety-net 加 `ALTER TABLE rag_query_logs ADD COLUMN citations_json TEXT`
  （`DO $$ ... IF NOT EXISTS $$` idempotent pattern；alembic 不在部署自動跑）。
- `alembic/versions/h2c3d4e5f6a7_*.py`：正式 migration（記錄用）。

### 前端

- `src/lib/api.ts`：
  - `askRag` 移除危險預設 upn `'global_test@company.com'`，未登入直接擋（**R-E3**，防跨租戶外洩）；
    加 `signal?: AbortSignal`（R-B1）。
  - `RagHistoryItem` interface 加 `citations?: RagCitation[]`（R-A1）。
- `src/components/rag/ChatPanel.tsx`：
  - **R-A1**：history dropdown 載入歷史時還原 `citations: item.citations ?? []`。
  - **R-B1**：`abortRef` + `cancelQuery()`；`sendMessage` 建 AbortController 傳 signal；loading 指示器加「取消查詢」鈕；
    catch 分流 `AbortError`（靜默停止）vs 真錯。
  - **R-B2**：錯誤氣泡帶 `isError/retryQuery`，渲染「重試」鈕呼叫 `sendMessage(retryQuery)`；重試前先清舊錯誤氣泡。
  - **R-C1**：錯誤氣泡加「回報問題」鈕 → `window.dispatchEvent(CustomEvent('meetchi:open-feedback'))`；
    錯誤文案不再叫使用者去（手機看不到的）左側 sidebar。
  - **R-C3**：`greeting.meeting_count === 0` 顯示零會議專屬空狀態，引導先上傳。
  - **R-D2**：history dropdown confidence 英文→中文（高信心／低信心／查無資料）。
  - **R-F6**：welcome 與底部建議問句移除 RAG/ROI/KPI 生硬術語（「誰提過 RAG 架構？」→「最近有哪些重要決策？」）。
  - **R-A5**：citations similarity 判斷 `cite.similarity &&` → `!= null &&`（相似度 0 也顯示）。
- `src/app/dashboard/page.tsx`：加 `meetchi:open-feedback` 全域事件 listener → `setFeedbackContext({})`（R-C1）。

### 暫緩（架構級/後續批次）
R-A7+R-C4 meeting_ids 篩選+索引狀態、R-F2 SSE 串流、R-A4 引用逐段渲染重構、R-E1 引用前後文。

## 驗證狀態（物理證據）

- ✅ `npx tsc --noEmit`：0 錯誤。
- ✅ `npm run build`：Compiled successfully，8/8 靜態頁生成成功。
- ✅ 後端 `pytest tests/test_rag_*.py`：**63 passed**。
- 🚀 部署 backend：`meetchi-backend-00095-yoz`（100% 流量），`/health` → 200。
- 🚀 部署 frontend：`meetchi-frontend-00071-2rs`。
  - **⚠️ 再次遇到流量鎖定陷阱**：deploy 後流量仍停在舊 `00070-fzt`，
    手動 `gcloud run services update-traffic meetchi-frontend --to-revisions meetchi-frontend-00071-2rs=100` 才切換。
  - 冒煙：`/api/health` → 200、`/` → 200。
- ✅ 後端 schema 驗證：`GET /api/v1/rag/history?user_upn=...`（含 `citations_json` 欄位）→ 200 `[]`，
  確認 ALTER TABLE 已生效、SELECT 不 500。

## 時間
- UTC：2026-07-01 07:51
- UTC+8：2026-07-01 15:51

---

## 批次 3：模板管理 UX 優化 — 大部分完成並上線

依 `docs/audits/2026-06-30-module-template-management-4persona-audit.md` 實作。T-A1（P0，上傳套用模板）
盤點後發現已由既有 `UploadSettingsModal` 實作，其餘 P1/P2 集中在 `TemplateGallery.tsx`。

### 主要變更（`TemplateGallery.tsx` 重構）
- **T-A3/T-F4**：動態 `bg-${color}` → `COLOR_MAP` 靜態 class（brand-cta/brand-accent/status-success/
  status-warning），杜絕生產 build 被 Tailwind purge 導致底色失效。已驗證 4 色皆存在於輸出 CSS。
- **T-A2**：模板建立/複製/刪除/設預設後 `dispatchEvent('meetchi:templates-changed')`；
  `page.tsx`、`DetailView.tsx` 各自 `getTemplates` 加 listener 重抓，解決三處不同步。
- **T-A4**：Header「+ 新增模板」→ 從零進空白編輯器（create 模式，共用 TemplateEditor）。
- **T-A5**：編輯器開放 icon（10 選）/ color（4 選）選擇，color dot 用靜態 `dot` class 防 purge。
- **T-A6**：搜尋對名稱/說明/標籤皆 `toLowerCase`。
- **T-B1**：Fork 完 toast 成功 + action「立即編輯」直接進編輯器。
- **T-B2**：「⭐ 設為預設」存 `localStorage['meetchi:default_template']`，卡片顯示「⭐ 預設」徽章；
  `page.tsx` 初始化 `uploadTemplateName` 讀預設 + 監聽變更 → 上傳設定視窗自動帶入。
- **T-B3**：儲存/建立/複製/刪除全程 `toast.loading → success/error` 回饋。
- **T-B4/T-E2**：刪除改單一對話框，直接顯示「已被 N 場會議使用」使用數。
- **T-C1**：編輯器前端驗證——空名稱、無段落、段落空標題/空指令/空或重複 `output_key` 標紅擋送。
- **T-C2**：instruction `maxLength=500` + 即時字數計數器（超限紅字）。
- **T-C3**：dirty-check——有未存變更時取消/Esc 彈「放棄修改？」確認。
- **T-C5**：空狀態分流（搜尋/篩選無結果→清除；完全無模板→新增 CTA）。
- **T-D1**：Gallery 過濾 `is_active !== false`，與 DetailView/上傳下游一致。
- **T-D3**：自訂模板加「我的」徽章（與「系統」對稱）。
- **T-E1**：刪除文案改「30 天內可請 IT 協助還原」（誠實反映 soft delete 無自助還原）。
- **T-F1**：Preview/Delete/Fork/Editor 全套 `useEscape` 支援 Esc 關閉。
- **T-F2**：刪除鈕加「刪除」文字。**T-F3**：段落雙欄窄螢幕改單欄。
- **T-F5**：預覽指令超 100 字可展開；段落顯示友善輸出型別（一段文字/條列清單/結構化欄位）。
- **T-F6**：`usage_count` 改用 `TemplateDTO.usage_count`，移除 `(tpl as any)`。

### 暫緩（架構級/IA/需後端偏好）
T-B5 dry-run 範例輸出（需後端）、T-B6 模板移工作區（IA 調整）、T-D2 抽共用下拉元件、
T-D4 語彙全面統一、T-E3 隱藏系統模板（偏好層）、T-E4 簡易模式（大改版）。

### 驗證狀態（物理證據）
- ✅ `npx tsc --noEmit`：0 錯誤。
- ✅ `npm run build`：Compiled successfully in 94s，8/8 靜態頁。
- ✅ 輸出 CSS 顏色 class 檢查：brand-accent 4 / status-success 95 / status-warning 53 / brand-cta 485 occurrences（無 purge）。
- 🚀 部署 frontend：`meetchi-frontend-00072-t4x`。
  - **⚠️ 第三次遇到流量鎖定陷阱**：deploy 後流量停在舊 `00071-2rs`，手動 update-traffic 到 `00072-t4x` 才上線。
  - 冒煙：`/` 200、`/api/health` 200、backend `/health` 200。
- 批次 3 為純前端，未動後端。

### 時間
- UTC：2026-07-01 08:xx / UTC+8：2026-07-01 16:xx
