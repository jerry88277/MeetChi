# ⚠️ DEPRECATED — DO NOT USE THIS FILE
# ========================================
# This file has an INCORRECT default URL: http://127.0.0.1:8000
# Use cloudbuild-frontend.yaml instead.
#
# Correct command:
#   gcloud builds submit --config apps/frontend/cloudbuild-frontend.yaml \
#     apps/frontend --substitutions=_IMAGE_TAG="v6"
#
# See: cloudbuild-frontend.yaml for the canonical build configuration.

substitutions:
  _NEXT_PUBLIC_API_URL: "https://meetchi-backend-705495828555.asia-southeast1.run.app"
  _IMAGE_TAG: "asia-southeast1-docker.pkg.dev/project-51769b5e-7f0f-4a2f-80c/meetchi/meetchi-frontend:latest"

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_API_URL=${_NEXT_PUBLIC_API_URL}'
      - '-t'
      - '${_IMAGE_TAG}'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_IMAGE_TAG}'

images:
  - '${_IMAGE_TAG}'

timeout: '900s'
